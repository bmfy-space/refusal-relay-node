{
  "name": "refusal-relay-node",
  "version": "1.0.0",
  "description": "An Express-based relay proxy that detects refusal responses, rewrites the conversation, and retries upstream LLM requests.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  }
}
